The test cases found in this directory are believed to be compressed
optimally.  Some of them are tricky to compress in an optimal way,
because the compressor must look beyound the data it's corrently
analyzing to determine what action to take.
